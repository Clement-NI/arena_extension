apiVersion: v1
kind: Service
metadata:
  labels:
    logstash.service: logstash-service
  name: logstash-service
spec:
  ports:
  - name: "5400"
    port: 5400
    targetPort: 5400
  selector:
    node.type: logstash-node
